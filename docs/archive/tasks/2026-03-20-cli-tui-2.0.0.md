# 2026-03-20 cli-tui-2.0.0

- 状态：COMPLETED
- 所属阶段：Phase 8 / CLI/TUI 首版
- 对应范围：统一 Maven 版本到 `2.0.0`，并继续收尾 `ai4j-cli` 的 CLI/TUI 体验
- 关联文档：
  - `docs/archive/tasks/2026-03-19-ai4j-cli-minimal-coding-session.md`
  - `docs/archive/tasks/2026-03-20-ai4j-cli-finish.md`

## 1. 目标

在不扩大首版范围的前提下，完成两项当前最必要的收敛：

1. 所有 Maven 模块版本从 `1.4.3` 切换到 `2.0.0`
2. `ai4j-cli` 在现有 `code` 命令基础上补一个最小可用的 `tui` 模式，而不是只停留在普通行式交互

## 2. 任务拆解

| 编号 | 任务 | 状态 | 说明 |
| --- | --- | --- | --- |
| T1 | 创建当前功能任务文档 | COMPLETED | 防止本轮实现漂移 |
| T2 | 统一 Maven 版本到 `2.0.0` | COMPLETED | root / parent / bom / starter / cli 等全部同步 |
| T3 | 为 `ai4j-cli` 增加最小 TUI 模式 | COMPLETED | 已支持 `tui` 顶层命令与 `--ui tui` |
| T4 | 优化 CLI/TUI 交互与帮助输出 | COMPLETED | 已增加状态、清屏、compact、模式标识等基础体验 |
| T5 | 补充 CLI/TUI 本地测试 | COMPLETED | 已覆盖顶层分发、TUI 路由、非法参数、compact |
| T6 | 完成模块测试与打包验证 | COMPLETED | 已完成本地单测与 `jar-with-dependencies` 打包 |

## 3. 变更记录

- 2026-03-20：创建任务文档，锁定“2.0.0 版本统一 + CLI/TUI 最小增强”。
- 2026-03-20：完成版本升级、最小 TUI、session compact 命令、本地测试与模块打包验证。
