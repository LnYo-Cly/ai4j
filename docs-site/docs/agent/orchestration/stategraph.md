# 页面已迁移

Workflow / StateGraph 的正式文档已经迁移到 `Agent` 顶级 canonical 页面：

- [Workflow StateGraph](/docs/agent/workflow-stategraph)

这个旧页面仅保留兼容旧链接，不再维护第二套正文。

迁移后，StateGraph、SubAgent、Teams 都统一收口到 `agent/*` 顶级页面，避免 `orchestration/*` 和顶级页面双份漂移。
