# Tool Whitelist and Security

工具接入最重要的原则不是“能不能调”，而是“默认暴露哪些”。

## 1. 默认原则

- 传什么，暴露什么
- 不显式暴露，就不应该自动可见

## 2. 为什么必须白名单

如果工具暴露范围过宽，最容易出问题的是：

- 高风险写操作
- 非预期本地文件读取
- 把本地 MCP 工具全量泄露给模型

## 3. 工程建议

- 把查询类工具和写操作工具分开考虑
- 并行工具调用默认对副作用工具保持保守
- 在上层 runtime 中再叠加审批与沙箱
