# Positioning and When to Use

这一页只回答一个问题：什么时候应该用 MCP。

## 1. 用本地 `Tool`

当能力就在本地 Java 进程里，而且你只需要稳定的函数调用语义时。

## 2. 用 `Skill`

当你要沉淀方法论、模板或可按需读取的任务说明时。

## 3. 用 `MCP`

当你需要：

- 连接外部能力系统
- 标准化 transport
- 管理多个服务
- 发布自己的能力给别人消费

就应该进入 MCP。
