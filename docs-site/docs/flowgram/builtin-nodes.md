---
unlisted: true
---

# Flowgram 内置节点

本页保留给旧链接。

正式文档请看：

- [Built-in Nodes](/docs/flowgram/built-in-nodes)

如果你要找的是：

- 内核节点与 executor 节点的分层
- `LLM` / `HTTP` / `CODE` / `TOOL` / `KNOWLEDGE` 的真实输入输出 contract
- 为什么 `START` / `END` / `CONDITION` / `LOOP` 属于 runtime 语义

都以 [Built-in Nodes](/docs/flowgram/built-in-nodes) 为准。
